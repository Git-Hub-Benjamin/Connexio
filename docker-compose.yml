version: '3.8'

services:
  connexio:
    build: ./server
    container_name: connexio-server
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - connexio-data:/data
    environment:
      - TZ=America/Denver  # Utah timezone

volumes:
  connexio-data:
